# Control LitMotion Animation from C#

You can control the created LitMotion Animation from C#.

```cs
LitMotionAnimation animation;

// Play (or resume) the animation
animation.Play();

// Pause the animation
animation.Pause();

// Stop the animation
animation.Stop();

// Restart the animation from the beginning
animation.Restart();
```
