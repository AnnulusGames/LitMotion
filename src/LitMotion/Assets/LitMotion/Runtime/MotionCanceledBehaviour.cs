namespace LitMotion
{
    /// <summary>
    /// Specifies the behavior when await is canceled.
    /// </summary>
    public enum MotionCanceledBehavior : byte
    {
        None,
        Cancel,
        Complete
    }
}