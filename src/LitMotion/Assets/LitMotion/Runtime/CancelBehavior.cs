namespace LitMotion
{
    /// <summary>
    /// Specifies the behavior when await is canceled.
    /// </summary>
    public enum CancelBehavior : byte
    {
        None,
        Cancel,
        Complete
    }
}