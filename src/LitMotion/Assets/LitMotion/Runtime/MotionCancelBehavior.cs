namespace LitMotion
{
    /// <summary>
    /// Specifies the behavior when await is canceled.
    /// </summary>
    public enum MotionCancelBehavior : byte
    {
        None,
        Cancel,
        Complete
    }
}